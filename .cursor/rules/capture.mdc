---
description: 捕获一个网页并保存到本地
globs: []
alwaysApply: false
---

用户想要捕获一个网页。你的任务：

1. 执行 `pnpm setup` 检查环境
2. 确认通过后执行 `pnpm site:capture -- <url>`
3. 读取 `sites/` 下新增目录的 `reports/capture-summary.md`
4. 向用户总结捕获结果
