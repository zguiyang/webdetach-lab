---
description: 检查项目环境依赖是否完整
---

Run `pnpm setup` and report results for each dependency.

If any FAIL items exist, provide fix instructions. If all pass, suggest next steps:

- pnpm site:capture -- <url>  capture a site
- pnpm site:serve -- <name>    preview a site
