---
description: 捕获一个网页并保存到本地
---

Execute `pnpm setup` to check environment. If passed, run:

pnpm site:capture -- "$ARGUMENTS"

After capture, read the newly created site directory's `reports/capture-summary.md` and summarize the results (requests count, resources, screenshots, gaps).
