---
description: 本地预览已复刻的网站
---

First list all captured sites: `ls sites/`

If `$ARGUMENTS` is empty, list sites and ask user to choose. Otherwise run:

pnpm site:serve -- "$ARGUMENTS"

Tell user the server address (default http://localhost:4173) and how to verify.
